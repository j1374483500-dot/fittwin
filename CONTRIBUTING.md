# Contributing

Please open an issue before proposing a large API or data-contract change. Keep the core package local-only and free of personal-data inference. Every public behavior change needs tests and documentation.

Run `pnpm typecheck && pnpm test && pnpm build` before opening a pull request. Never commit API keys, real body profiles, provider responses containing personal data, or generated body imagery.
