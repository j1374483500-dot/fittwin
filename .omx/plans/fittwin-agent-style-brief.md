# FitTwin: agent style brief

Add `fittwin_generate_style_brief` to the local MCP toolset. It reuses the existing deterministic, schema-validated style advisor so an AI client can request useful styling guidance without a model credential, browser request, or network connection. Verify through the real stdio MCP client that its structured response contains outfit recipes.
